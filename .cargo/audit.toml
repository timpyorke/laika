[advisories]
# `rsa` is absent from the supported Windows release target (`cargo tree
# --target x86_64-pc-windows-msvc -i rsa` is empty) and the advisory has no
# fixed release. Remove this exception before shipping another target.
ignore = ["RUSTSEC-2023-0071"]
